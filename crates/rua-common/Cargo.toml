[package]
name = "rua-common"
version = "0.1.1"
edition = "2024"
description = "Protocol-neutral shared types, lexer, project model, and standard library resources for Rua."

[lib]
name = "rua_common"
path = "src/lib.rs"
doctest = false

[dependencies]
serde = { workspace = true }
toml = { workspace = true }
include_dir = { workspace = true }

[dev-dependencies]
proptest = "1"
