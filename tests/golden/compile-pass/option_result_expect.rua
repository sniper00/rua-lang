let optional: Option<i64> = Option::Some(7);
let outcome: Result<i64, String> = Result::Ok(9);

println!(
    "{} {}",
    optional.expect("missing option"),
    outcome.expect("failed operation"),
);
