# Phase 4A TODO Goldens

These fixtures are intentionally outside the active compile-pass/fail
directories. Their inventory is enforced by
phase4a_todo_goldens_are_registered; the pass runner remains ignored until
parser, type checking, and codegen steps graduate each case into the active
golden corpus.

The accepted behavior and unsupported boundary are defined in
docs/rua-closure-iterator-rfc.md.
